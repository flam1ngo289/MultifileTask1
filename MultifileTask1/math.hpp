#ifndef math_hpp
#define math_hpp

//#include <stdio.h>

int sum(int &a, int &b);
int sub(int &a, int &b);
int multyply(int &a, int &b);
int division(int &a, int &b);
int pow(int &a, int &b);

#endif
